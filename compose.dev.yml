services:
  app:
    container_name: goods-go-dev
    image: goods-go-dev
    build:
      context: .
      dockerfile: dev.Dockerfile
    environment:
      NODE_ENV: development
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    ports:
      - "3000:3000"
    restart: unless-stopped
