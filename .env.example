# Production Cloudflare Tunnel token file used by Docker Compose secrets.
TUNNEL_TOKEN_FILE=./secrets/cloudflare-tunnel-token

# Optional: override self-host DB connection for prod migration/lint/plan
SUPABASE_DB_URL=
